// Tailwind v4 is handled by the @tailwindcss/vite plugin in vite.config.js —
// no PostCSS plugin configuration is needed. This file is kept to satisfy any
// tooling that expects it to exist, but it declares no plugins.
export default {}
