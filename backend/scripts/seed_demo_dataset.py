"""
Standardify — Demo dataset seeder.

Runs the complete ingestion pipeline end-to-end on the final curated
8–12 BIS PDFs placed in data/raw_pdfs/, so the entire backend can be
rebuilt from scratch on a clean machine (or Render) with one command.

Usage:
  python scripts/seed_demo_dataset.py

Phase 0 stub: no logic yet — implemented in Phase 10.4.
"""
from __future__ import annotations
